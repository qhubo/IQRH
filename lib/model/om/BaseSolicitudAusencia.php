<?php


/**
 * Base class that represents a row from the 'solicitud_ausencia' table.
 *
 *
 *
 * This class was autogenerated by Propel 1.6.7 on:
 *
 * 02/14/19 20:14:53
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseSolicitudAusencia extends BaseObject implements Persistent
{
    /**
     * Peer class name
     */
    const PEER = 'SolicitudAusenciaPeer';

    /**
     * The Peer class.
     * Instance provides a convenient way of calling static methods on a class
     * that calling code may not be able to identify.
     * @var        SolicitudAusenciaPeer
     */
    protected static $peer;

    /**
     * The flag var to prevent infinit loop in deep copy
     * @var       boolean
     */
    protected $startCopy = false;

    /**
     * The value for the id field.
     * @var        int
     */
    protected $id;

    /**
     * The value for the usuario_id field.
     * @var        int
     */
    protected $usuario_id;

    /**
     * The value for the fecha field.
     * @var        string
     */
    protected $fecha;

    /**
     * The value for the motivo field.
     * @var        string
     */
    protected $motivo;

    /**
     * The value for the observaciones field.
     * @var        string
     */
    protected $observaciones;

    /**
     * The value for the estado field.
     * Note: this column has a database default value of: 'Pendiente'
     * @var        string
     */
    protected $estado;

    /**
     * The value for the created_at field.
     * @var        string
     */
    protected $created_at;

    /**
     * The value for the updated_at field.
     * @var        string
     */
    protected $updated_at;

    /**
     * The value for the jefe field.
     * @var        int
     */
    protected $jefe;

    /**
     * The value for the usuario_modero field.
     * @var        string
     */
    protected $usuario_modero;

    /**
     * The value for the comentario_modero field.
     * @var        string
     */
    protected $comentario_modero;

    /**
     * The value for the archivo_uno field.
     * @var        string
     */
    protected $archivo_uno;

    /**
     * The value for the archivo_dos field.
     * @var        string
     */
    protected $archivo_dos;

    /**
     * The value for the fecha_fin field.
     * @var        string
     */
    protected $fecha_fin;

    /**
     * The value for the enviado_correo field.
     * Note: this column has a database default value of: false
     * @var        boolean
     */
    protected $enviado_correo;

    /**
     * @var        Usuario
     */
    protected $aUsuario;

    /**
     * @var        PropelObjectCollection|AusenciaDetalle[] Collection to store aggregation of AusenciaDetalle objects.
     */
    protected $collAusenciaDetalles;
    protected $collAusenciaDetallesPartial;

    /**
     * Flag to prevent endless save loop, if this object is referenced
     * by another object which falls in this transaction.
     * @var        boolean
     */
    protected $alreadyInSave = false;

    /**
     * Flag to prevent endless validation loop, if this object is referenced
     * by another object which falls in this transaction.
     * @var        boolean
     */
    protected $alreadyInValidation = false;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $ausenciaDetallesScheduledForDeletion = null;

    /**
     * Applies default values to this object.
     * This method should be called from the object's constructor (or
     * equivalent initialization method).
     * @see        __construct()
     */
    public function applyDefaultValues()
    {
        $this->estado = 'Pendiente';
        $this->enviado_correo = false;
    }

    /**
     * Initializes internal state of BaseSolicitudAusencia object.
     * @see        applyDefaults()
     */
    public function __construct()
    {
        parent::__construct();
        $this->applyDefaultValues();
    }

    /**
     * Get the [id] column value.
     *
     * @return int
     */
    public function getId()
    {
        return $this->id;
    }

    /**
     * Get the [usuario_id] column value.
     *
     * @return int
     */
    public function getUsuarioId()
    {
        return $this->usuario_id;
    }

    /**
     * Get the [optionally formatted] temporal [fecha] column value.
     *
     *
     * @param string $format The date/time format string (either date()-style or strftime()-style).
     *				 If format is null, then the raw DateTime object will be returned.
     * @return mixed Formatted date/time value as string or DateTime object (if format is null), null if column is null, and 0 if column value is 0000-00-00
     * @throws PropelException - if unable to parse/validate the date/time value.
     */
    public function getFecha($format = 'Y-m-d')
    {
        if ($this->fecha === null) {
            return null;
        }

        if ($this->fecha === '0000-00-00') {
            // while technically this is not a default value of null,
            // this seems to be closest in meaning.
            return null;
        } else {
            try {
                $dt = new DateTime($this->fecha);
            } catch (Exception $x) {
                throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->fecha, true), $x);
            }
        }

        if ($format === null) {
            // Because propel.useDateTimeClass is true, we return a DateTime object.
            return $dt;
        } elseif (strpos($format, '%') !== false) {
            return strftime($format, $dt->format('U'));
        } else {
            return $dt->format($format);
        }
    }

    /**
     * Get the [motivo] column value.
     *
     * @return string
     */
    public function getMotivo()
    {
        return $this->motivo;
    }

    /**
     * Get the [observaciones] column value.
     *
     * @return string
     */
    public function getObservaciones()
    {
        return $this->observaciones;
    }

    /**
     * Get the [estado] column value.
     *
     * @return string
     */
    public function getEstado()
    {
        return $this->estado;
    }

    /**
     * Get the [optionally formatted] temporal [created_at] column value.
     *
     *
     * @param string $format The date/time format string (either date()-style or strftime()-style).
     *				 If format is null, then the raw DateTime object will be returned.
     * @return mixed Formatted date/time value as string or DateTime object (if format is null), null if column is null, and 0 if column value is 0000-00-00 00:00:00
     * @throws PropelException - if unable to parse/validate the date/time value.
     */
    public function getCreatedAt($format = 'Y-m-d H:i:s')
    {
        if ($this->created_at === null) {
            return null;
        }

        if ($this->created_at === '0000-00-00 00:00:00') {
            // while technically this is not a default value of null,
            // this seems to be closest in meaning.
            return null;
        } else {
            try {
                $dt = new DateTime($this->created_at);
            } catch (Exception $x) {
                throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->created_at, true), $x);
            }
        }

        if ($format === null) {
            // Because propel.useDateTimeClass is true, we return a DateTime object.
            return $dt;
        } elseif (strpos($format, '%') !== false) {
            return strftime($format, $dt->format('U'));
        } else {
            return $dt->format($format);
        }
    }

    /**
     * Get the [optionally formatted] temporal [updated_at] column value.
     *
     *
     * @param string $format The date/time format string (either date()-style or strftime()-style).
     *				 If format is null, then the raw DateTime object will be returned.
     * @return mixed Formatted date/time value as string or DateTime object (if format is null), null if column is null, and 0 if column value is 0000-00-00 00:00:00
     * @throws PropelException - if unable to parse/validate the date/time value.
     */
    public function getUpdatedAt($format = 'Y-m-d H:i:s')
    {
        if ($this->updated_at === null) {
            return null;
        }

        if ($this->updated_at === '0000-00-00 00:00:00') {
            // while technically this is not a default value of null,
            // this seems to be closest in meaning.
            return null;
        } else {
            try {
                $dt = new DateTime($this->updated_at);
            } catch (Exception $x) {
                throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->updated_at, true), $x);
            }
        }

        if ($format === null) {
            // Because propel.useDateTimeClass is true, we return a DateTime object.
            return $dt;
        } elseif (strpos($format, '%') !== false) {
            return strftime($format, $dt->format('U'));
        } else {
            return $dt->format($format);
        }
    }

    /**
     * Get the [jefe] column value.
     *
     * @return int
     */
    public function getJefe()
    {
        return $this->jefe;
    }

    /**
     * Get the [usuario_modero] column value.
     *
     * @return string
     */
    public function getUsuarioModero()
    {
        return $this->usuario_modero;
    }

    /**
     * Get the [comentario_modero] column value.
     *
     * @return string
     */
    public function getComentarioModero()
    {
        return $this->comentario_modero;
    }

    /**
     * Get the [archivo_uno] column value.
     *
     * @return string
     */
    public function getArchivoUno()
    {
        return $this->archivo_uno;
    }

    /**
     * Get the [archivo_dos] column value.
     *
     * @return string
     */
    public function getArchivoDos()
    {
        return $this->archivo_dos;
    }

    /**
     * Get the [optionally formatted] temporal [fecha_fin] column value.
     *
     *
     * @param string $format The date/time format string (either date()-style or strftime()-style).
     *				 If format is null, then the raw DateTime object will be returned.
     * @return mixed Formatted date/time value as string or DateTime object (if format is null), null if column is null, and 0 if column value is 0000-00-00
     * @throws PropelException - if unable to parse/validate the date/time value.
     */
    public function getFechaFin($format = 'Y-m-d')
    {
        if ($this->fecha_fin === null) {
            return null;
        }

        if ($this->fecha_fin === '0000-00-00') {
            // while technically this is not a default value of null,
            // this seems to be closest in meaning.
            return null;
        } else {
            try {
                $dt = new DateTime($this->fecha_fin);
            } catch (Exception $x) {
                throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->fecha_fin, true), $x);
            }
        }

        if ($format === null) {
            // Because propel.useDateTimeClass is true, we return a DateTime object.
            return $dt;
        } elseif (strpos($format, '%') !== false) {
            return strftime($format, $dt->format('U'));
        } else {
            return $dt->format($format);
        }
    }

    /**
     * Get the [enviado_correo] column value.
     *
     * @return boolean
     */
    public function getEnviadoCorreo()
    {
        return $this->enviado_correo;
    }

    /**
     * Set the value of [id] column.
     *
     * @param int $v new value
     * @return SolicitudAusencia The current object (for fluent API support)
     */
    public function setId($v)
    {
        if ($v !== null) {
            $v = (int) $v;
        }

        if ($this->id !== $v) {
            $this->id = $v;
            $this->modifiedColumns[] = SolicitudAusenciaPeer::ID;
        }


        return $this;
    } // setId()

    /**
     * Set the value of [usuario_id] column.
     *
     * @param int $v new value
     * @return SolicitudAusencia The current object (for fluent API support)
     */
    public function setUsuarioId($v)
    {
        if ($v !== null) {
            $v = (int) $v;
        }

        if ($this->usuario_id !== $v) {
            $this->usuario_id = $v;
            $this->modifiedColumns[] = SolicitudAusenciaPeer::USUARIO_ID;
        }

        if ($this->aUsuario !== null && $this->aUsuario->getId() !== $v) {
            $this->aUsuario = null;
        }


        return $this;
    } // setUsuarioId()

    /**
     * Sets the value of [fecha] column to a normalized version of the date/time value specified.
     *
     * @param mixed $v string, integer (timestamp), or DateTime value.
     *               Empty strings are treated as null.
     * @return SolicitudAusencia The current object (for fluent API support)
     */
    public function setFecha($v)
    {
        $dt = PropelDateTime::newInstance($v, null, 'DateTime');
        if ($this->fecha !== null || $dt !== null) {
            $currentDateAsString = ($this->fecha !== null && $tmpDt = new DateTime($this->fecha)) ? $tmpDt->format('Y-m-d') : null;
            $newDateAsString = $dt ? $dt->format('Y-m-d') : null;
            if ($currentDateAsString !== $newDateAsString) {
                $this->fecha = $newDateAsString;
                $this->modifiedColumns[] = SolicitudAusenciaPeer::FECHA;
            }
        } // if either are not null


        return $this;
    } // setFecha()

    /**
     * Set the value of [motivo] column.
     *
     * @param string $v new value
     * @return SolicitudAusencia The current object (for fluent API support)
     */
    public function setMotivo($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->motivo !== $v) {
            $this->motivo = $v;
            $this->modifiedColumns[] = SolicitudAusenciaPeer::MOTIVO;
        }


        return $this;
    } // setMotivo()

    /**
     * Set the value of [observaciones] column.
     *
     * @param string $v new value
     * @return SolicitudAusencia The current object (for fluent API support)
     */
    public function setObservaciones($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->observaciones !== $v) {
            $this->observaciones = $v;
            $this->modifiedColumns[] = SolicitudAusenciaPeer::OBSERVACIONES;
        }


        return $this;
    } // setObservaciones()

    /**
     * Set the value of [estado] column.
     *
     * @param string $v new value
     * @return SolicitudAusencia The current object (for fluent API support)
     */
    public function setEstado($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->estado !== $v) {
            $this->estado = $v;
            $this->modifiedColumns[] = SolicitudAusenciaPeer::ESTADO;
        }


        return $this;
    } // setEstado()

    /**
     * Sets the value of [created_at] column to a normalized version of the date/time value specified.
     *
     * @param mixed $v string, integer (timestamp), or DateTime value.
     *               Empty strings are treated as null.
     * @return SolicitudAusencia The current object (for fluent API support)
     */
    public function setCreatedAt($v)
    {
        $dt = PropelDateTime::newInstance($v, null, 'DateTime');
        if ($this->created_at !== null || $dt !== null) {
            $currentDateAsString = ($this->created_at !== null && $tmpDt = new DateTime($this->created_at)) ? $tmpDt->format('Y-m-d H:i:s') : null;
            $newDateAsString = $dt ? $dt->format('Y-m-d H:i:s') : null;
            if ($currentDateAsString !== $newDateAsString) {
                $this->created_at = $newDateAsString;
                $this->modifiedColumns[] = SolicitudAusenciaPeer::CREATED_AT;
            }
        } // if either are not null


        return $this;
    } // setCreatedAt()

    /**
     * Sets the value of [updated_at] column to a normalized version of the date/time value specified.
     *
     * @param mixed $v string, integer (timestamp), or DateTime value.
     *               Empty strings are treated as null.
     * @return SolicitudAusencia The current object (for fluent API support)
     */
    public function setUpdatedAt($v)
    {
        $dt = PropelDateTime::newInstance($v, null, 'DateTime');
        if ($this->updated_at !== null || $dt !== null) {
            $currentDateAsString = ($this->updated_at !== null && $tmpDt = new DateTime($this->updated_at)) ? $tmpDt->format('Y-m-d H:i:s') : null;
            $newDateAsString = $dt ? $dt->format('Y-m-d H:i:s') : null;
            if ($currentDateAsString !== $newDateAsString) {
                $this->updated_at = $newDateAsString;
                $this->modifiedColumns[] = SolicitudAusenciaPeer::UPDATED_AT;
            }
        } // if either are not null


        return $this;
    } // setUpdatedAt()

    /**
     * Set the value of [jefe] column.
     *
     * @param int $v new value
     * @return SolicitudAusencia The current object (for fluent API support)
     */
    public function setJefe($v)
    {
        if ($v !== null) {
            $v = (int) $v;
        }

        if ($this->jefe !== $v) {
            $this->jefe = $v;
            $this->modifiedColumns[] = SolicitudAusenciaPeer::JEFE;
        }


        return $this;
    } // setJefe()

    /**
     * Set the value of [usuario_modero] column.
     *
     * @param string $v new value
     * @return SolicitudAusencia The current object (for fluent API support)
     */
    public function setUsuarioModero($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->usuario_modero !== $v) {
            $this->usuario_modero = $v;
            $this->modifiedColumns[] = SolicitudAusenciaPeer::USUARIO_MODERO;
        }


        return $this;
    } // setUsuarioModero()

    /**
     * Set the value of [comentario_modero] column.
     *
     * @param string $v new value
     * @return SolicitudAusencia The current object (for fluent API support)
     */
    public function setComentarioModero($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->comentario_modero !== $v) {
            $this->comentario_modero = $v;
            $this->modifiedColumns[] = SolicitudAusenciaPeer::COMENTARIO_MODERO;
        }


        return $this;
    } // setComentarioModero()

    /**
     * Set the value of [archivo_uno] column.
     *
     * @param string $v new value
     * @return SolicitudAusencia The current object (for fluent API support)
     */
    public function setArchivoUno($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->archivo_uno !== $v) {
            $this->archivo_uno = $v;
            $this->modifiedColumns[] = SolicitudAusenciaPeer::ARCHIVO_UNO;
        }


        return $this;
    } // setArchivoUno()

    /**
     * Set the value of [archivo_dos] column.
     *
     * @param string $v new value
     * @return SolicitudAusencia The current object (for fluent API support)
     */
    public function setArchivoDos($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->archivo_dos !== $v) {
            $this->archivo_dos = $v;
            $this->modifiedColumns[] = SolicitudAusenciaPeer::ARCHIVO_DOS;
        }


        return $this;
    } // setArchivoDos()

    /**
     * Sets the value of [fecha_fin] column to a normalized version of the date/time value specified.
     *
     * @param mixed $v string, integer (timestamp), or DateTime value.
     *               Empty strings are treated as null.
     * @return SolicitudAusencia The current object (for fluent API support)
     */
    public function setFechaFin($v)
    {
        $dt = PropelDateTime::newInstance($v, null, 'DateTime');
        if ($this->fecha_fin !== null || $dt !== null) {
            $currentDateAsString = ($this->fecha_fin !== null && $tmpDt = new DateTime($this->fecha_fin)) ? $tmpDt->format('Y-m-d') : null;
            $newDateAsString = $dt ? $dt->format('Y-m-d') : null;
            if ($currentDateAsString !== $newDateAsString) {
                $this->fecha_fin = $newDateAsString;
                $this->modifiedColumns[] = SolicitudAusenciaPeer::FECHA_FIN;
            }
        } // if either are not null


        return $this;
    } // setFechaFin()

    /**
     * Sets the value of the [enviado_correo] column.
     * Non-boolean arguments are converted using the following rules:
     *   * 1, '1', 'true',  'on',  and 'yes' are converted to boolean true
     *   * 0, '0', 'false', 'off', and 'no'  are converted to boolean false
     * Check on string values is case insensitive (so 'FaLsE' is seen as 'false').
     *
     * @param boolean|integer|string $v The new value
     * @return SolicitudAusencia The current object (for fluent API support)
     */
    public function setEnviadoCorreo($v)
    {
        if ($v !== null) {
            if (is_string($v)) {
                $v = in_array(strtolower($v), array('false', 'off', '-', 'no', 'n', '0', '')) ? false : true;
            } else {
                $v = (boolean) $v;
            }
        }

        if ($this->enviado_correo !== $v) {
            $this->enviado_correo = $v;
            $this->modifiedColumns[] = SolicitudAusenciaPeer::ENVIADO_CORREO;
        }


        return $this;
    } // setEnviadoCorreo()

    /**
     * Indicates whether the columns in this object are only set to default values.
     *
     * This method can be used in conjunction with isModified() to indicate whether an object is both
     * modified _and_ has some values set which are non-default.
     *
     * @return boolean Whether the columns in this object are only been set with default values.
     */
    public function hasOnlyDefaultValues()
    {
            if ($this->estado !== 'Pendiente') {
                return false;
            }

            if ($this->enviado_correo !== false) {
                return false;
            }

        // otherwise, everything was equal, so return true
        return true;
    } // hasOnlyDefaultValues()

    /**
     * Hydrates (populates) the object variables with values from the database resultset.
     *
     * An offset (0-based "start column") is specified so that objects can be hydrated
     * with a subset of the columns in the resultset rows.  This is needed, for example,
     * for results of JOIN queries where the resultset row includes columns from two or
     * more tables.
     *
     * @param array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
     * @param int $startcol 0-based offset column which indicates which restultset column to start with.
     * @param boolean $rehydrate Whether this object is being re-hydrated from the database.
     * @return int             next starting column
     * @throws PropelException - Any caught Exception will be rewrapped as a PropelException.
     */
    public function hydrate($row, $startcol = 0, $rehydrate = false)
    {
        try {

            $this->id = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
            $this->usuario_id = ($row[$startcol + 1] !== null) ? (int) $row[$startcol + 1] : null;
            $this->fecha = ($row[$startcol + 2] !== null) ? (string) $row[$startcol + 2] : null;
            $this->motivo = ($row[$startcol + 3] !== null) ? (string) $row[$startcol + 3] : null;
            $this->observaciones = ($row[$startcol + 4] !== null) ? (string) $row[$startcol + 4] : null;
            $this->estado = ($row[$startcol + 5] !== null) ? (string) $row[$startcol + 5] : null;
            $this->created_at = ($row[$startcol + 6] !== null) ? (string) $row[$startcol + 6] : null;
            $this->updated_at = ($row[$startcol + 7] !== null) ? (string) $row[$startcol + 7] : null;
            $this->jefe = ($row[$startcol + 8] !== null) ? (int) $row[$startcol + 8] : null;
            $this->usuario_modero = ($row[$startcol + 9] !== null) ? (string) $row[$startcol + 9] : null;
            $this->comentario_modero = ($row[$startcol + 10] !== null) ? (string) $row[$startcol + 10] : null;
            $this->archivo_uno = ($row[$startcol + 11] !== null) ? (string) $row[$startcol + 11] : null;
            $this->archivo_dos = ($row[$startcol + 12] !== null) ? (string) $row[$startcol + 12] : null;
            $this->fecha_fin = ($row[$startcol + 13] !== null) ? (string) $row[$startcol + 13] : null;
            $this->enviado_correo = ($row[$startcol + 14] !== null) ? (boolean) $row[$startcol + 14] : null;
            $this->resetModified();

            $this->setNew(false);

            if ($rehydrate) {
                $this->ensureConsistency();
            }

            return $startcol + 15; // 15 = SolicitudAusenciaPeer::NUM_HYDRATE_COLUMNS.

        } catch (Exception $e) {
            throw new PropelException("Error populating SolicitudAusencia object", $e);
        }
    }

    /**
     * Checks and repairs the internal consistency of the object.
     *
     * This method is executed after an already-instantiated object is re-hydrated
     * from the database.  It exists to check any foreign keys to make sure that
     * the objects related to the current object are correct based on foreign key.
     *
     * You can override this method in the stub class, but you should always invoke
     * the base method from the overridden method (i.e. parent::ensureConsistency()),
     * in case your model changes.
     *
     * @throws PropelException
     */
    public function ensureConsistency()
    {

        if ($this->aUsuario !== null && $this->usuario_id !== $this->aUsuario->getId()) {
            $this->aUsuario = null;
        }
    } // ensureConsistency

    /**
     * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
     *
     * This will only work if the object has been saved and has a valid primary key set.
     *
     * @param boolean $deep (optional) Whether to also de-associated any related objects.
     * @param PropelPDO $con (optional) The PropelPDO connection to use.
     * @return void
     * @throws PropelException - if this object is deleted, unsaved or doesn't have pk match in db
     */
    public function reload($deep = false, PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("Cannot reload a deleted object.");
        }

        if ($this->isNew()) {
            throw new PropelException("Cannot reload an unsaved object.");
        }

        if ($con === null) {
            $con = Propel::getConnection(SolicitudAusenciaPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }

        // We don't need to alter the object instance pool; we're just modifying this instance
        // already in the pool.

        $stmt = SolicitudAusenciaPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
        $row = $stmt->fetch(PDO::FETCH_NUM);
        $stmt->closeCursor();
        if (!$row) {
            throw new PropelException('Cannot find matching row in the database to reload object values.');
        }
        $this->hydrate($row, 0, true); // rehydrate

        if ($deep) {  // also de-associate any related objects?

            $this->aUsuario = null;
            $this->collAusenciaDetalles = null;

        } // if (deep)
    }

    /**
     * Removes this object from datastore and sets delete attribute.
     *
     * @param PropelPDO $con
     * @return void
     * @throws PropelException
     * @throws Exception
     * @see        BaseObject::setDeleted()
     * @see        BaseObject::isDeleted()
     */
    public function delete(PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("This object has already been deleted.");
        }

        if ($con === null) {
            $con = Propel::getConnection(SolicitudAusenciaPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
        }

        $con->beginTransaction();
        try {
            $deleteQuery = SolicitudAusenciaQuery::create()
                ->filterByPrimaryKey($this->getPrimaryKey());
            $ret = $this->preDelete($con);
            // symfony_behaviors behavior
            foreach (sfMixer::getCallables('BaseSolicitudAusencia:delete:pre') as $callable)
            {
              if (call_user_func($callable, $this, $con))
              {
                $con->commit();
                return;
              }
            }

            if ($ret) {
                $deleteQuery->delete($con);
                $this->postDelete($con);
                // symfony_behaviors behavior
                foreach (sfMixer::getCallables('BaseSolicitudAusencia:delete:post') as $callable)
                {
                  call_user_func($callable, $this, $con);
                }

                $con->commit();
                $this->setDeleted(true);
            } else {
                $con->commit();
            }
        } catch (Exception $e) {
            $con->rollBack();
            throw $e;
        }
    }

    /**
     * Persists this object to the database.
     *
     * If the object is new, it inserts it; otherwise an update is performed.
     * All modified related objects will also be persisted in the doSave()
     * method.  This method wraps all precipitate database operations in a
     * single transaction.
     *
     * @param PropelPDO $con
     * @return int             The number of rows affected by this insert/update and any referring fk objects' save() operations.
     * @throws PropelException
     * @throws Exception
     * @see        doSave()
     */
    public function save(PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("You cannot save an object that has been deleted.");
        }

        if ($con === null) {
            $con = Propel::getConnection(SolicitudAusenciaPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
        }

        $con->beginTransaction();
        $isInsert = $this->isNew();
        try {
            $ret = $this->preSave($con);
            // symfony_behaviors behavior
            foreach (sfMixer::getCallables('BaseSolicitudAusencia:save:pre') as $callable)
            {
              if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
              {
                  $con->commit();
                return $affectedRows;
              }
            }

            // symfony_timestampable behavior
            if ($this->isModified() && !$this->isColumnModified(SolicitudAusenciaPeer::UPDATED_AT))
            {
                $this->setUpdatedAt(time());
            }
            if ($isInsert) {
                $ret = $ret && $this->preInsert($con);
                // symfony_timestampable behavior
                if (!$this->isColumnModified(SolicitudAusenciaPeer::CREATED_AT))
                {
                  $this->setCreatedAt(time());
                }

            } else {
                $ret = $ret && $this->preUpdate($con);
            }
            if ($ret) {
                $affectedRows = $this->doSave($con);
                if ($isInsert) {
                    $this->postInsert($con);
                } else {
                    $this->postUpdate($con);
                }
                $this->postSave($con);
                // symfony_behaviors behavior
                foreach (sfMixer::getCallables('BaseSolicitudAusencia:save:post') as $callable)
                {
                  call_user_func($callable, $this, $con, $affectedRows);
                }

                SolicitudAusenciaPeer::addInstanceToPool($this);
            } else {
                $affectedRows = 0;
            }
            $con->commit();

            return $affectedRows;
        } catch (Exception $e) {
            $con->rollBack();
            throw $e;
        }
    }

    /**
     * Performs the work of inserting or updating the row in the database.
     *
     * If the object is new, it inserts it; otherwise an update is performed.
     * All related objects are also updated in this method.
     *
     * @param PropelPDO $con
     * @return int             The number of rows affected by this insert/update and any referring fk objects' save() operations.
     * @throws PropelException
     * @see        save()
     */
    protected function doSave(PropelPDO $con)
    {
        $affectedRows = 0; // initialize var to track total num of affected rows
        if (!$this->alreadyInSave) {
            $this->alreadyInSave = true;

            // We call the save method on the following object(s) if they
            // were passed to this object by their coresponding set
            // method.  This object relates to these object(s) by a
            // foreign key reference.

            if ($this->aUsuario !== null) {
                if ($this->aUsuario->isModified() || $this->aUsuario->isNew()) {
                    $affectedRows += $this->aUsuario->save($con);
                }
                $this->setUsuario($this->aUsuario);
            }

            if ($this->isNew() || $this->isModified()) {
                // persist changes
                if ($this->isNew()) {
                    $this->doInsert($con);
                } else {
                    $this->doUpdate($con);
                }
                $affectedRows += 1;
                $this->resetModified();
            }

            if ($this->ausenciaDetallesScheduledForDeletion !== null) {
                if (!$this->ausenciaDetallesScheduledForDeletion->isEmpty()) {
                    foreach ($this->ausenciaDetallesScheduledForDeletion as $ausenciaDetalle) {
                        // need to save related object because we set the relation to null
                        $ausenciaDetalle->save($con);
                    }
                    $this->ausenciaDetallesScheduledForDeletion = null;
                }
            }

            if ($this->collAusenciaDetalles !== null) {
                foreach ($this->collAusenciaDetalles as $referrerFK) {
                    if (!$referrerFK->isDeleted()) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            $this->alreadyInSave = false;

        }

        return $affectedRows;
    } // doSave()

    /**
     * Insert the row in the database.
     *
     * @param PropelPDO $con
     *
     * @throws PropelException
     * @see        doSave()
     */
    protected function doInsert(PropelPDO $con)
    {
        $modifiedColumns = array();
        $index = 0;

        $this->modifiedColumns[] = SolicitudAusenciaPeer::ID;
        if (null !== $this->id) {
            throw new PropelException('Cannot insert a value for auto-increment primary key (' . SolicitudAusenciaPeer::ID . ')');
        }

         // check the columns in natural order for more readable SQL queries
        if ($this->isColumnModified(SolicitudAusenciaPeer::ID)) {
            $modifiedColumns[':p' . $index++]  = '`ID`';
        }
        if ($this->isColumnModified(SolicitudAusenciaPeer::USUARIO_ID)) {
            $modifiedColumns[':p' . $index++]  = '`USUARIO_ID`';
        }
        if ($this->isColumnModified(SolicitudAusenciaPeer::FECHA)) {
            $modifiedColumns[':p' . $index++]  = '`FECHA`';
        }
        if ($this->isColumnModified(SolicitudAusenciaPeer::MOTIVO)) {
            $modifiedColumns[':p' . $index++]  = '`MOTIVO`';
        }
        if ($this->isColumnModified(SolicitudAusenciaPeer::OBSERVACIONES)) {
            $modifiedColumns[':p' . $index++]  = '`OBSERVACIONES`';
        }
        if ($this->isColumnModified(SolicitudAusenciaPeer::ESTADO)) {
            $modifiedColumns[':p' . $index++]  = '`ESTADO`';
        }
        if ($this->isColumnModified(SolicitudAusenciaPeer::CREATED_AT)) {
            $modifiedColumns[':p' . $index++]  = '`CREATED_AT`';
        }
        if ($this->isColumnModified(SolicitudAusenciaPeer::UPDATED_AT)) {
            $modifiedColumns[':p' . $index++]  = '`UPDATED_AT`';
        }
        if ($this->isColumnModified(SolicitudAusenciaPeer::JEFE)) {
            $modifiedColumns[':p' . $index++]  = '`JEFE`';
        }
        if ($this->isColumnModified(SolicitudAusenciaPeer::USUARIO_MODERO)) {
            $modifiedColumns[':p' . $index++]  = '`USUARIO_MODERO`';
        }
        if ($this->isColumnModified(SolicitudAusenciaPeer::COMENTARIO_MODERO)) {
            $modifiedColumns[':p' . $index++]  = '`COMENTARIO_MODERO`';
        }
        if ($this->isColumnModified(SolicitudAusenciaPeer::ARCHIVO_UNO)) {
            $modifiedColumns[':p' . $index++]  = '`ARCHIVO_UNO`';
        }
        if ($this->isColumnModified(SolicitudAusenciaPeer::ARCHIVO_DOS)) {
            $modifiedColumns[':p' . $index++]  = '`ARCHIVO_DOS`';
        }
        if ($this->isColumnModified(SolicitudAusenciaPeer::FECHA_FIN)) {
            $modifiedColumns[':p' . $index++]  = '`FECHA_FIN`';
        }
        if ($this->isColumnModified(SolicitudAusenciaPeer::ENVIADO_CORREO)) {
            $modifiedColumns[':p' . $index++]  = '`ENVIADO_CORREO`';
        }

        $sql = sprintf(
            'INSERT INTO `solicitud_ausencia` (%s) VALUES (%s)',
            implode(', ', $modifiedColumns),
            implode(', ', array_keys($modifiedColumns))
        );

        try {
            $stmt = $con->prepare($sql);
            foreach ($modifiedColumns as $identifier => $columnName) {
                switch ($columnName) {
                    case '`ID`':
                        $stmt->bindValue($identifier, $this->id, PDO::PARAM_INT);
                        break;
                    case '`USUARIO_ID`':
                        $stmt->bindValue($identifier, $this->usuario_id, PDO::PARAM_INT);
                        break;
                    case '`FECHA`':
                        $stmt->bindValue($identifier, $this->fecha, PDO::PARAM_STR);
                        break;
                    case '`MOTIVO`':
                        $stmt->bindValue($identifier, $this->motivo, PDO::PARAM_STR);
                        break;
                    case '`OBSERVACIONES`':
                        $stmt->bindValue($identifier, $this->observaciones, PDO::PARAM_STR);
                        break;
                    case '`ESTADO`':
                        $stmt->bindValue($identifier, $this->estado, PDO::PARAM_STR);
                        break;
                    case '`CREATED_AT`':
                        $stmt->bindValue($identifier, $this->created_at, PDO::PARAM_STR);
                        break;
                    case '`UPDATED_AT`':
                        $stmt->bindValue($identifier, $this->updated_at, PDO::PARAM_STR);
                        break;
                    case '`JEFE`':
                        $stmt->bindValue($identifier, $this->jefe, PDO::PARAM_INT);
                        break;
                    case '`USUARIO_MODERO`':
                        $stmt->bindValue($identifier, $this->usuario_modero, PDO::PARAM_STR);
                        break;
                    case '`COMENTARIO_MODERO`':
                        $stmt->bindValue($identifier, $this->comentario_modero, PDO::PARAM_STR);
                        break;
                    case '`ARCHIVO_UNO`':
                        $stmt->bindValue($identifier, $this->archivo_uno, PDO::PARAM_STR);
                        break;
                    case '`ARCHIVO_DOS`':
                        $stmt->bindValue($identifier, $this->archivo_dos, PDO::PARAM_STR);
                        break;
                    case '`FECHA_FIN`':
                        $stmt->bindValue($identifier, $this->fecha_fin, PDO::PARAM_STR);
                        break;
                    case '`ENVIADO_CORREO`':
                        $stmt->bindValue($identifier, (int) $this->enviado_correo, PDO::PARAM_INT);
                        break;
                }
            }
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute INSERT statement [%s]', $sql), $e);
        }

        try {
            $pk = $con->lastInsertId();
        } catch (Exception $e) {
            throw new PropelException('Unable to get autoincrement id.', $e);
        }
        $this->setId($pk);

        $this->setNew(false);
    }

    /**
     * Update the row in the database.
     *
     * @param PropelPDO $con
     *
     * @see        doSave()
     */
    protected function doUpdate(PropelPDO $con)
    {
        $selectCriteria = $this->buildPkeyCriteria();
        $valuesCriteria = $this->buildCriteria();
        BasePeer::doUpdate($selectCriteria, $valuesCriteria, $con);
    }

    /**
     * Array of ValidationFailed objects.
     * @var        array ValidationFailed[]
     */
    protected $validationFailures = array();

    /**
     * Gets any ValidationFailed objects that resulted from last call to validate().
     *
     *
     * @return array ValidationFailed[]
     * @see        validate()
     */
    public function getValidationFailures()
    {
        return $this->validationFailures;
    }

    /**
     * Validates the objects modified field values and all objects related to this table.
     *
     * If $columns is either a column name or an array of column names
     * only those columns are validated.
     *
     * @param mixed $columns Column name or an array of column names.
     * @return boolean Whether all columns pass validation.
     * @see        doValidate()
     * @see        getValidationFailures()
     */
    public function validate($columns = null)
    {
        $res = $this->doValidate($columns);
        if ($res === true) {
            $this->validationFailures = array();

            return true;
        } else {
            $this->validationFailures = $res;

            return false;
        }
    }

    /**
     * This function performs the validation work for complex object models.
     *
     * In addition to checking the current object, all related objects will
     * also be validated.  If all pass then <code>true</code> is returned; otherwise
     * an aggreagated array of ValidationFailed objects will be returned.
     *
     * @param array $columns Array of column names to validate.
     * @return mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
     */
    protected function doValidate($columns = null)
    {
        if (!$this->alreadyInValidation) {
            $this->alreadyInValidation = true;
            $retval = null;

            $failureMap = array();


            // We call the validate method on the following object(s) if they
            // were passed to this object by their coresponding set
            // method.  This object relates to these object(s) by a
            // foreign key reference.

            if ($this->aUsuario !== null) {
                if (!$this->aUsuario->validate($columns)) {
                    $failureMap = array_merge($failureMap, $this->aUsuario->getValidationFailures());
                }
            }


            if (($retval = SolicitudAusenciaPeer::doValidate($this, $columns)) !== true) {
                $failureMap = array_merge($failureMap, $retval);
            }


                if ($this->collAusenciaDetalles !== null) {
                    foreach ($this->collAusenciaDetalles as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }


            $this->alreadyInValidation = false;
        }

        return (!empty($failureMap) ? $failureMap : true);
    }

    /**
     * Retrieves a field from the object by name passed in as a string.
     *
     * @param string $name name
     * @param string $type The type of fieldname the $name is of:
     *               one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
     *               BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *               Defaults to BasePeer::TYPE_PHPNAME
     * @return mixed Value of field.
     */
    public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
    {
        $pos = SolicitudAusenciaPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
        $field = $this->getByPosition($pos);

        return $field;
    }

    /**
     * Retrieves a field from the object by Position as specified in the xml schema.
     * Zero-based.
     *
     * @param int $pos position in xml schema
     * @return mixed Value of field at $pos
     */
    public function getByPosition($pos)
    {
        switch ($pos) {
            case 0:
                return $this->getId();
                break;
            case 1:
                return $this->getUsuarioId();
                break;
            case 2:
                return $this->getFecha();
                break;
            case 3:
                return $this->getMotivo();
                break;
            case 4:
                return $this->getObservaciones();
                break;
            case 5:
                return $this->getEstado();
                break;
            case 6:
                return $this->getCreatedAt();
                break;
            case 7:
                return $this->getUpdatedAt();
                break;
            case 8:
                return $this->getJefe();
                break;
            case 9:
                return $this->getUsuarioModero();
                break;
            case 10:
                return $this->getComentarioModero();
                break;
            case 11:
                return $this->getArchivoUno();
                break;
            case 12:
                return $this->getArchivoDos();
                break;
            case 13:
                return $this->getFechaFin();
                break;
            case 14:
                return $this->getEnviadoCorreo();
                break;
            default:
                return null;
                break;
        } // switch()
    }

    /**
     * Exports the object as an array.
     *
     * You can specify the key type of the array by passing one of the class
     * type constants.
     *
     * @param     string  $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
     *                    BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *                    Defaults to BasePeer::TYPE_PHPNAME.
     * @param     boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns. Defaults to true.
     * @param     array $alreadyDumpedObjects List of objects to skip to avoid recursion
     * @param     boolean $includeForeignObjects (optional) Whether to include hydrated related objects. Default to FALSE.
     *
     * @return array an associative array containing the field names (as keys) and field values
     */
    public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true, $alreadyDumpedObjects = array(), $includeForeignObjects = false)
    {
        if (isset($alreadyDumpedObjects['SolicitudAusencia'][$this->getPrimaryKey()])) {
            return '*RECURSION*';
        }
        $alreadyDumpedObjects['SolicitudAusencia'][$this->getPrimaryKey()] = true;
        $keys = SolicitudAusenciaPeer::getFieldNames($keyType);
        $result = array(
            $keys[0] => $this->getId(),
            $keys[1] => $this->getUsuarioId(),
            $keys[2] => $this->getFecha(),
            $keys[3] => $this->getMotivo(),
            $keys[4] => $this->getObservaciones(),
            $keys[5] => $this->getEstado(),
            $keys[6] => $this->getCreatedAt(),
            $keys[7] => $this->getUpdatedAt(),
            $keys[8] => $this->getJefe(),
            $keys[9] => $this->getUsuarioModero(),
            $keys[10] => $this->getComentarioModero(),
            $keys[11] => $this->getArchivoUno(),
            $keys[12] => $this->getArchivoDos(),
            $keys[13] => $this->getFechaFin(),
            $keys[14] => $this->getEnviadoCorreo(),
        );
        if ($includeForeignObjects) {
            if (null !== $this->aUsuario) {
                $result['Usuario'] = $this->aUsuario->toArray($keyType, $includeLazyLoadColumns,  $alreadyDumpedObjects, true);
            }
            if (null !== $this->collAusenciaDetalles) {
                $result['AusenciaDetalles'] = $this->collAusenciaDetalles->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
        }

        return $result;
    }

    /**
     * Sets a field from the object by name passed in as a string.
     *
     * @param string $name peer name
     * @param mixed $value field value
     * @param string $type The type of fieldname the $name is of:
     *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
     *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *                     Defaults to BasePeer::TYPE_PHPNAME
     * @return void
     */
    public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
    {
        $pos = SolicitudAusenciaPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);

        $this->setByPosition($pos, $value);
    }

    /**
     * Sets a field from the object by Position as specified in the xml schema.
     * Zero-based.
     *
     * @param int $pos position in xml schema
     * @param mixed $value field value
     * @return void
     */
    public function setByPosition($pos, $value)
    {
        switch ($pos) {
            case 0:
                $this->setId($value);
                break;
            case 1:
                $this->setUsuarioId($value);
                break;
            case 2:
                $this->setFecha($value);
                break;
            case 3:
                $this->setMotivo($value);
                break;
            case 4:
                $this->setObservaciones($value);
                break;
            case 5:
                $this->setEstado($value);
                break;
            case 6:
                $this->setCreatedAt($value);
                break;
            case 7:
                $this->setUpdatedAt($value);
                break;
            case 8:
                $this->setJefe($value);
                break;
            case 9:
                $this->setUsuarioModero($value);
                break;
            case 10:
                $this->setComentarioModero($value);
                break;
            case 11:
                $this->setArchivoUno($value);
                break;
            case 12:
                $this->setArchivoDos($value);
                break;
            case 13:
                $this->setFechaFin($value);
                break;
            case 14:
                $this->setEnviadoCorreo($value);
                break;
        } // switch()
    }

    /**
     * Populates the object using an array.
     *
     * This is particularly useful when populating an object from one of the
     * request arrays (e.g. $_POST).  This method goes through the column
     * names, checking to see whether a matching key exists in populated
     * array. If so the setByName() method is called for that column.
     *
     * You can specify the key type of the array by additionally passing one
     * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
     * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     * The default key type is the column's BasePeer::TYPE_PHPNAME
     *
     * @param array  $arr     An array to populate the object from.
     * @param string $keyType The type of keys the array uses.
     * @return void
     */
    public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
    {
        $keys = SolicitudAusenciaPeer::getFieldNames($keyType);

        if (array_key_exists($keys[0], $arr)) $this->setId($arr[$keys[0]]);
        if (array_key_exists($keys[1], $arr)) $this->setUsuarioId($arr[$keys[1]]);
        if (array_key_exists($keys[2], $arr)) $this->setFecha($arr[$keys[2]]);
        if (array_key_exists($keys[3], $arr)) $this->setMotivo($arr[$keys[3]]);
        if (array_key_exists($keys[4], $arr)) $this->setObservaciones($arr[$keys[4]]);
        if (array_key_exists($keys[5], $arr)) $this->setEstado($arr[$keys[5]]);
        if (array_key_exists($keys[6], $arr)) $this->setCreatedAt($arr[$keys[6]]);
        if (array_key_exists($keys[7], $arr)) $this->setUpdatedAt($arr[$keys[7]]);
        if (array_key_exists($keys[8], $arr)) $this->setJefe($arr[$keys[8]]);
        if (array_key_exists($keys[9], $arr)) $this->setUsuarioModero($arr[$keys[9]]);
        if (array_key_exists($keys[10], $arr)) $this->setComentarioModero($arr[$keys[10]]);
        if (array_key_exists($keys[11], $arr)) $this->setArchivoUno($arr[$keys[11]]);
        if (array_key_exists($keys[12], $arr)) $this->setArchivoDos($arr[$keys[12]]);
        if (array_key_exists($keys[13], $arr)) $this->setFechaFin($arr[$keys[13]]);
        if (array_key_exists($keys[14], $arr)) $this->setEnviadoCorreo($arr[$keys[14]]);
    }

    /**
     * Build a Criteria object containing the values of all modified columns in this object.
     *
     * @return Criteria The Criteria object containing all modified values.
     */
    public function buildCriteria()
    {
        $criteria = new Criteria(SolicitudAusenciaPeer::DATABASE_NAME);

        if ($this->isColumnModified(SolicitudAusenciaPeer::ID)) $criteria->add(SolicitudAusenciaPeer::ID, $this->id);
        if ($this->isColumnModified(SolicitudAusenciaPeer::USUARIO_ID)) $criteria->add(SolicitudAusenciaPeer::USUARIO_ID, $this->usuario_id);
        if ($this->isColumnModified(SolicitudAusenciaPeer::FECHA)) $criteria->add(SolicitudAusenciaPeer::FECHA, $this->fecha);
        if ($this->isColumnModified(SolicitudAusenciaPeer::MOTIVO)) $criteria->add(SolicitudAusenciaPeer::MOTIVO, $this->motivo);
        if ($this->isColumnModified(SolicitudAusenciaPeer::OBSERVACIONES)) $criteria->add(SolicitudAusenciaPeer::OBSERVACIONES, $this->observaciones);
        if ($this->isColumnModified(SolicitudAusenciaPeer::ESTADO)) $criteria->add(SolicitudAusenciaPeer::ESTADO, $this->estado);
        if ($this->isColumnModified(SolicitudAusenciaPeer::CREATED_AT)) $criteria->add(SolicitudAusenciaPeer::CREATED_AT, $this->created_at);
        if ($this->isColumnModified(SolicitudAusenciaPeer::UPDATED_AT)) $criteria->add(SolicitudAusenciaPeer::UPDATED_AT, $this->updated_at);
        if ($this->isColumnModified(SolicitudAusenciaPeer::JEFE)) $criteria->add(SolicitudAusenciaPeer::JEFE, $this->jefe);
        if ($this->isColumnModified(SolicitudAusenciaPeer::USUARIO_MODERO)) $criteria->add(SolicitudAusenciaPeer::USUARIO_MODERO, $this->usuario_modero);
        if ($this->isColumnModified(SolicitudAusenciaPeer::COMENTARIO_MODERO)) $criteria->add(SolicitudAusenciaPeer::COMENTARIO_MODERO, $this->comentario_modero);
        if ($this->isColumnModified(SolicitudAusenciaPeer::ARCHIVO_UNO)) $criteria->add(SolicitudAusenciaPeer::ARCHIVO_UNO, $this->archivo_uno);
        if ($this->isColumnModified(SolicitudAusenciaPeer::ARCHIVO_DOS)) $criteria->add(SolicitudAusenciaPeer::ARCHIVO_DOS, $this->archivo_dos);
        if ($this->isColumnModified(SolicitudAusenciaPeer::FECHA_FIN)) $criteria->add(SolicitudAusenciaPeer::FECHA_FIN, $this->fecha_fin);
        if ($this->isColumnModified(SolicitudAusenciaPeer::ENVIADO_CORREO)) $criteria->add(SolicitudAusenciaPeer::ENVIADO_CORREO, $this->enviado_correo);

        return $criteria;
    }

    /**
     * Builds a Criteria object containing the primary key for this object.
     *
     * Unlike buildCriteria() this method includes the primary key values regardless
     * of whether or not they have been modified.
     *
     * @return Criteria The Criteria object containing value(s) for primary key(s).
     */
    public function buildPkeyCriteria()
    {
        $criteria = new Criteria(SolicitudAusenciaPeer::DATABASE_NAME);
        $criteria->add(SolicitudAusenciaPeer::ID, $this->id);

        return $criteria;
    }

    /**
     * Returns the primary key for this object (row).
     * @return int
     */
    public function getPrimaryKey()
    {
        return $this->getId();
    }

    /**
     * Generic method to set the primary key (id column).
     *
     * @param  int $key Primary key.
     * @return void
     */
    public function setPrimaryKey($key)
    {
        $this->setId($key);
    }

    /**
     * Returns true if the primary key for this object is null.
     * @return boolean
     */
    public function isPrimaryKeyNull()
    {

        return null === $this->getId();
    }

    /**
     * Sets contents of passed object to values from current object.
     *
     * If desired, this method can also make copies of all associated (fkey referrers)
     * objects.
     *
     * @param object $copyObj An object of SolicitudAusencia (or compatible) type.
     * @param boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
     * @param boolean $makeNew Whether to reset autoincrement PKs and make the object new.
     * @throws PropelException
     */
    public function copyInto($copyObj, $deepCopy = false, $makeNew = true)
    {
        $copyObj->setUsuarioId($this->getUsuarioId());
        $copyObj->setFecha($this->getFecha());
        $copyObj->setMotivo($this->getMotivo());
        $copyObj->setObservaciones($this->getObservaciones());
        $copyObj->setEstado($this->getEstado());
        $copyObj->setCreatedAt($this->getCreatedAt());
        $copyObj->setUpdatedAt($this->getUpdatedAt());
        $copyObj->setJefe($this->getJefe());
        $copyObj->setUsuarioModero($this->getUsuarioModero());
        $copyObj->setComentarioModero($this->getComentarioModero());
        $copyObj->setArchivoUno($this->getArchivoUno());
        $copyObj->setArchivoDos($this->getArchivoDos());
        $copyObj->setFechaFin($this->getFechaFin());
        $copyObj->setEnviadoCorreo($this->getEnviadoCorreo());

        if ($deepCopy && !$this->startCopy) {
            // important: temporarily setNew(false) because this affects the behavior of
            // the getter/setter methods for fkey referrer objects.
            $copyObj->setNew(false);
            // store object hash to prevent cycle
            $this->startCopy = true;

            foreach ($this->getAusenciaDetalles() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addAusenciaDetalle($relObj->copy($deepCopy));
                }
            }

            //unflag object copy
            $this->startCopy = false;
        } // if ($deepCopy)

        if ($makeNew) {
            $copyObj->setNew(true);
            $copyObj->setId(NULL); // this is a auto-increment column, so set to default value
        }
    }

    /**
     * Makes a copy of this object that will be inserted as a new row in table when saved.
     * It creates a new object filling in the simple attributes, but skipping any primary
     * keys that are defined for the table.
     *
     * If desired, this method can also make copies of all associated (fkey referrers)
     * objects.
     *
     * @param boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
     * @return SolicitudAusencia Clone of current object.
     * @throws PropelException
     */
    public function copy($deepCopy = false)
    {
        // we use get_class(), because this might be a subclass
        $clazz = get_class($this);
        $copyObj = new $clazz();
        $this->copyInto($copyObj, $deepCopy);

        return $copyObj;
    }

    /**
     * Returns a peer instance associated with this om.
     *
     * Since Peer classes are not to have any instance attributes, this method returns the
     * same instance for all member of this class. The method could therefore
     * be static, but this would prevent one from overriding the behavior.
     *
     * @return SolicitudAusenciaPeer
     */
    public function getPeer()
    {
        if (self::$peer === null) {
            self::$peer = new SolicitudAusenciaPeer();
        }

        return self::$peer;
    }

    /**
     * Declares an association between this object and a Usuario object.
     *
     * @param             Usuario $v
     * @return SolicitudAusencia The current object (for fluent API support)
     * @throws PropelException
     */
    public function setUsuario(Usuario $v = null)
    {
        if ($v === null) {
            $this->setUsuarioId(NULL);
        } else {
            $this->setUsuarioId($v->getId());
        }

        $this->aUsuario = $v;

        // Add binding for other direction of this n:n relationship.
        // If this object has already been added to the Usuario object, it will not be re-added.
        if ($v !== null) {
            $v->addSolicitudAusencia($this);
        }


        return $this;
    }


    /**
     * Get the associated Usuario object
     *
     * @param PropelPDO $con Optional Connection object.
     * @return Usuario The associated Usuario object.
     * @throws PropelException
     */
    public function getUsuario(PropelPDO $con = null)
    {
        if ($this->aUsuario === null && ($this->usuario_id !== null)) {
            $this->aUsuario = UsuarioQuery::create()->findPk($this->usuario_id, $con);
            /* The following can be used additionally to
                guarantee the related object contains a reference
                to this object.  This level of coupling may, however, be
                undesirable since it could result in an only partially populated collection
                in the referenced object.
                $this->aUsuario->addSolicitudAusencias($this);
             */
        }

        return $this->aUsuario;
    }


    /**
     * Initializes a collection based on the name of a relation.
     * Avoids crafting an 'init[$relationName]s' method name
     * that wouldn't work when StandardEnglishPluralizer is used.
     *
     * @param string $relationName The name of the relation to initialize
     * @return void
     */
    public function initRelation($relationName)
    {
        if ('AusenciaDetalle' == $relationName) {
            $this->initAusenciaDetalles();
        }
    }

    /**
     * Clears out the collAusenciaDetalles collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return void
     * @see        addAusenciaDetalles()
     */
    public function clearAusenciaDetalles()
    {
        $this->collAusenciaDetalles = null; // important to set this to null since that means it is uninitialized
        $this->collAusenciaDetallesPartial = null;
    }

    /**
     * reset is the collAusenciaDetalles collection loaded partially
     *
     * @return void
     */
    public function resetPartialAusenciaDetalles($v = true)
    {
        $this->collAusenciaDetallesPartial = $v;
    }

    /**
     * Initializes the collAusenciaDetalles collection.
     *
     * By default this just sets the collAusenciaDetalles collection to an empty array (like clearcollAusenciaDetalles());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initAusenciaDetalles($overrideExisting = true)
    {
        if (null !== $this->collAusenciaDetalles && !$overrideExisting) {
            return;
        }
        $this->collAusenciaDetalles = new PropelObjectCollection();
        $this->collAusenciaDetalles->setModel('AusenciaDetalle');
    }

    /**
     * Gets an array of AusenciaDetalle objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this SolicitudAusencia is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|AusenciaDetalle[] List of AusenciaDetalle objects
     * @throws PropelException
     */
    public function getAusenciaDetalles($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collAusenciaDetallesPartial && !$this->isNew();
        if (null === $this->collAusenciaDetalles || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collAusenciaDetalles) {
                // return empty collection
                $this->initAusenciaDetalles();
            } else {
                $collAusenciaDetalles = AusenciaDetalleQuery::create(null, $criteria)
                    ->filterBySolicitudAusencia($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collAusenciaDetallesPartial && count($collAusenciaDetalles)) {
                      $this->initAusenciaDetalles(false);

                      foreach($collAusenciaDetalles as $obj) {
                        if (false == $this->collAusenciaDetalles->contains($obj)) {
                          $this->collAusenciaDetalles->append($obj);
                        }
                      }

                      $this->collAusenciaDetallesPartial = true;
                    }

                    return $collAusenciaDetalles;
                }

                if($partial && $this->collAusenciaDetalles) {
                    foreach($this->collAusenciaDetalles as $obj) {
                        if($obj->isNew()) {
                            $collAusenciaDetalles[] = $obj;
                        }
                    }
                }

                $this->collAusenciaDetalles = $collAusenciaDetalles;
                $this->collAusenciaDetallesPartial = false;
            }
        }

        return $this->collAusenciaDetalles;
    }

    /**
     * Sets a collection of AusenciaDetalle objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $ausenciaDetalles A Propel collection.
     * @param PropelPDO $con Optional connection object
     */
    public function setAusenciaDetalles(PropelCollection $ausenciaDetalles, PropelPDO $con = null)
    {
        $this->ausenciaDetallesScheduledForDeletion = $this->getAusenciaDetalles(new Criteria(), $con)->diff($ausenciaDetalles);

        foreach ($this->ausenciaDetallesScheduledForDeletion as $ausenciaDetalleRemoved) {
            $ausenciaDetalleRemoved->setSolicitudAusencia(null);
        }

        $this->collAusenciaDetalles = null;
        foreach ($ausenciaDetalles as $ausenciaDetalle) {
            $this->addAusenciaDetalle($ausenciaDetalle);
        }

        $this->collAusenciaDetalles = $ausenciaDetalles;
        $this->collAusenciaDetallesPartial = false;
    }

    /**
     * Returns the number of related AusenciaDetalle objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related AusenciaDetalle objects.
     * @throws PropelException
     */
    public function countAusenciaDetalles(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collAusenciaDetallesPartial && !$this->isNew();
        if (null === $this->collAusenciaDetalles || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collAusenciaDetalles) {
                return 0;
            } else {
                if($partial && !$criteria) {
                    return count($this->getAusenciaDetalles());
                }
                $query = AusenciaDetalleQuery::create(null, $criteria);
                if ($distinct) {
                    $query->distinct();
                }

                return $query
                    ->filterBySolicitudAusencia($this)
                    ->count($con);
            }
        } else {
            return count($this->collAusenciaDetalles);
        }
    }

    /**
     * Method called to associate a AusenciaDetalle object to this object
     * through the AusenciaDetalle foreign key attribute.
     *
     * @param    AusenciaDetalle $l AusenciaDetalle
     * @return SolicitudAusencia The current object (for fluent API support)
     */
    public function addAusenciaDetalle(AusenciaDetalle $l)
    {
        if ($this->collAusenciaDetalles === null) {
            $this->initAusenciaDetalles();
            $this->collAusenciaDetallesPartial = true;
        }
        if (!$this->collAusenciaDetalles->contains($l)) { // only add it if the **same** object is not already associated
            $this->doAddAusenciaDetalle($l);
        }

        return $this;
    }

    /**
     * @param	AusenciaDetalle $ausenciaDetalle The ausenciaDetalle object to add.
     */
    protected function doAddAusenciaDetalle($ausenciaDetalle)
    {
        $this->collAusenciaDetalles[]= $ausenciaDetalle;
        $ausenciaDetalle->setSolicitudAusencia($this);
    }

    /**
     * @param	AusenciaDetalle $ausenciaDetalle The ausenciaDetalle object to remove.
     */
    public function removeAusenciaDetalle($ausenciaDetalle)
    {
        if ($this->getAusenciaDetalles()->contains($ausenciaDetalle)) {
            $this->collAusenciaDetalles->remove($this->collAusenciaDetalles->search($ausenciaDetalle));
            if (null === $this->ausenciaDetallesScheduledForDeletion) {
                $this->ausenciaDetallesScheduledForDeletion = clone $this->collAusenciaDetalles;
                $this->ausenciaDetallesScheduledForDeletion->clear();
            }
            $this->ausenciaDetallesScheduledForDeletion[]= $ausenciaDetalle;
            $ausenciaDetalle->setSolicitudAusencia(null);
        }
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this SolicitudAusencia is new, it will return
     * an empty collection; or if this SolicitudAusencia has previously
     * been saved, it will retrieve related AusenciaDetalles from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in SolicitudAusencia.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|AusenciaDetalle[] List of AusenciaDetalle objects
     */
    public function getAusenciaDetallesJoinUsuario($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = AusenciaDetalleQuery::create(null, $criteria);
        $query->joinWith('Usuario', $join_behavior);

        return $this->getAusenciaDetalles($query, $con);
    }

    /**
     * Clears the current object and sets all attributes to their default values
     */
    public function clear()
    {
        $this->id = null;
        $this->usuario_id = null;
        $this->fecha = null;
        $this->motivo = null;
        $this->observaciones = null;
        $this->estado = null;
        $this->created_at = null;
        $this->updated_at = null;
        $this->jefe = null;
        $this->usuario_modero = null;
        $this->comentario_modero = null;
        $this->archivo_uno = null;
        $this->archivo_dos = null;
        $this->fecha_fin = null;
        $this->enviado_correo = null;
        $this->alreadyInSave = false;
        $this->alreadyInValidation = false;
        $this->clearAllReferences();
        $this->applyDefaultValues();
        $this->resetModified();
        $this->setNew(true);
        $this->setDeleted(false);
    }

    /**
     * Resets all references to other model objects or collections of model objects.
     *
     * This method is a user-space workaround for PHP's inability to garbage collect
     * objects with circular references (even in PHP 5.3). This is currently necessary
     * when using Propel in certain daemon or large-volumne/high-memory operations.
     *
     * @param boolean $deep Whether to also clear the references on all referrer objects.
     */
    public function clearAllReferences($deep = false)
    {
        if ($deep) {
            if ($this->collAusenciaDetalles) {
                foreach ($this->collAusenciaDetalles as $o) {
                    $o->clearAllReferences($deep);
                }
            }
        } // if ($deep)

        if ($this->collAusenciaDetalles instanceof PropelCollection) {
            $this->collAusenciaDetalles->clearIterator();
        }
        $this->collAusenciaDetalles = null;
        $this->aUsuario = null;
    }

    /**
     * return the string representation of this object
     *
     * @return string
     */
    public function __toString()
    {
        return (string) $this->exportTo(SolicitudAusenciaPeer::DEFAULT_STRING_FORMAT);
    }

    /**
     * return true is the object is in saving state
     *
     * @return boolean
     */
    public function isAlreadyInSave()
    {
        return $this->alreadyInSave;
    }

    /**
     * Catches calls to virtual methods
     */
    public function __call($name, $params)
    {

        // symfony_behaviors behavior
        if ($callable = sfMixer::getCallable('BaseSolicitudAusencia:' . $name))
        {
          array_unshift($params, $this);
          return call_user_func_array($callable, $params);
        }


        return parent::__call($name, $params);
    }

}
